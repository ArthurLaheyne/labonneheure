<header>
    <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
        <a class="navbar-brand" href="{{ path('index') }}">Homepage</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item {{ app.request.get('_route') == 'demo' ? 'active' }}">
                    <a class="nav-link" href="{{ path('demo') }}">Demo</a>
                </li>
                <li class="nav-item {{ app.request.get('_route') == 'other_page' ? 'active' }}">
                    <a class="nav-link" href="{{ path('other_page') }}">Other page</a>
                </li>
            </ul>
        </div>
    </nav>
</header>
